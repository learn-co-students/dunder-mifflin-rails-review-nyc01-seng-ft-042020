<%if flash[:errors] %>
Sorry - you have some errors. Here they are:
<%flash[:errors].each do |error|%>
 <p><%=error%></p>
<%end%>
<%end%>


<%= form_for @employee do |f| %>
<%= f.label :first_name%>
<%= f.text_field :first_name%>
<%= f.label :last_name%>
<%= f.text_field :last_name%>
<%= f.label :title%>
<%= f.text_field :title%>
<%= f.label :alias%>
<%= f.text_field :alias%>
<%= f.label :office%>
<%= f.text_field :office%>
<%= f.label :img_url%>
<%= f.text_field :img_url%>
<%= f.collection_select(:dog_id, Dog.all, :id, :name)%>
<%= f.submit%>
<% end %>

<%= button_to "Delete Employee", employee_path, method: :delete%>